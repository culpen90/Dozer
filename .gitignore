build/
.build/
Releases/
Dozer.xcodeproj/
Credentials/

# Fastlane
fastlane/
fastlaneLegacy/
Gemfile
Gemfile.lock

# SwiftGen output is checked in so the app can build on Apple Silicon even
# when SwiftGen is not installed locally.
